apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "default-helm-app.fullname" . }}-test-connection"
  labels:
    {{- include "default-helm-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "default-helm-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
