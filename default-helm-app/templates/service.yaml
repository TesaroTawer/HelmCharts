apiVersion: v1
kind: Service
metadata:
  name: {{ include "default-helm-app.fullname" . }}
  labels:
    {{- include "default-helm-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "default-helm-app.selectorLabels" . | nindent 4 }}
